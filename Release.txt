Initial release before v1.0.

(v0.8)Most of the basic file explorer features are working perfectly and the binary currently only works with nvim.
Color config data with json files has been updated.
Still requires some work in fixing the ui elements and centralized configuratioin file integration.